class AI_Wordlist_Manager():
    def __init__(self):
        self.namePassword = []

    # [SRS 3.2.2.9.3] The AI Wordlist Manager Class shall add a wordlist provided by the analyst. 
    def addWordlist():
        return

    # [SRS 3.2.2.9.2] The AI Wordlist Manager Class shall create a list of possible usernames and passwords. 
    def createWordlist(self):
        return

    # [SRS 3.2.2.9.4] The AI Wordlist Manager Class shall be able to remove a previously created username and password list. 
    def removePreviousWordlist(self):
        return

    # [SRS 3.2.2.9.5] The AI Wordlist Manager Class shall save the results of creating the username and password list.  
    def saveWordlist(self):
        return

    # Maybe make this part of an interface class for all manager classes?
    # [SRS 3.2.2.9.7] The AI Wordlist Manager Class shall display the results of creating the username and password list. 
    def displayResults(self):
        return

    # Maybe make this part of an interface class for all manager classes?
    # [SRS 3.2.2.9.6] The AI Wordlist Manager Class shall be able to reset the service. 
    def resetService(self):
        return 